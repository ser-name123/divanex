"use client";

// Popups are completely disabled across the entire website per user specifications.
interface ConsultationModalProps {
  isOpen?: boolean;
  onClose?: () => void;
}

export default function ConsultationModal(_props?: ConsultationModalProps) {
  return null;
}
