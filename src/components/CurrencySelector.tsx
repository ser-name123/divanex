"use client";

// Currency is now 100% automatically detected based on the visitor's IP and Country.
// Manual currency switcher UI is deprecated and hidden.
export default function CurrencySelector() {
  return null;
}
